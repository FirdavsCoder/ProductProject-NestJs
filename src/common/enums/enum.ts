export enum RoleEnum {
  WORKER = 'worker',
  BOSS = 'boss',
  ADMIN = 'admin',
}

export enum AllUsers {
  ALLUSERS = 'AllUsers',
}

export enum AllProducts {
  ALLPRODUCTS = 'AllProducts',
}

export enum UserById {
  USERBYID = 'User_By_Id_',
}

export enum AllTransactions {
  ALLTRANSACTIONS = 'AllTransactions',
}
